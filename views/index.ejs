<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fast & Furious</title>
</head>
<body>
    <h1>Too Fast, Too Furious</h1>
    <img src="/img/Fast_and_Furious_Poster.jpeg" alt="Fast & Furious Image">
    <form action="/quotes" method="POST">
        <input type="text" placeholder="name" name="name">
        <input type="text" placeholder="quote" name="quote">
        <button type="submit">Submit</button>
    </form>
    <h2>Quotes</h2>

    <ul class="quotes">
      <!-- Loop through quotes -->
      <% for(var i = 0; i < quotes.length; i++) {%>
        <li class="quote">
          <!-- Output name from the iterated quote object -->
          <span><%= quotes[i].name %></span>:
          <!-- Output quote from the iterated quote object -->
          <span><%= quotes[i].quote %></span>
        </li>
      <% } %>
    </ul>
    <div>
        <h2>Make it a Brian Quote</h2>
        <p>
          Replace Dom's quote with a quote from Brian O'Conner
        </p>
        <button id="update-button">Replace Dom's Quote</button>
      </div>
    <div>
        <h2>Delete's Brian Quote</h2>
        <p>
          Delete one of Brian's quotes. Does nothing if there are no more Brian's quote.
        </p>
    <button id="delete-button">Delete Brian's Quote</button>
      </div>
      <div id="message"></div>
    <script src="/js/main.js"></script>
</body>
</html>